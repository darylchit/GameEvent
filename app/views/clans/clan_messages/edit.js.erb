var content = "<%= j render :partial => 'clan_messages/form', :locals => { :clan_message => @clan_message} %>";

(function(){
    $(document).on('ajax:success', '.edit_message', function(e){
        //$(e.target).parents('.message-container .dropdown-menu').dropdown('toggle');
        $(e.target).parents('.message-container').find('.message-body p').first().html(content);
        
    });
    
})();